# Firebase Configuration
# Get these values from Firebase Console > Project Settings > Your apps > Web app

VITE_FIREBASE_API_KEY=your_api_key_here

# IMPORTANT: Use your custom domain here, NOT your_project.firebaseapp.com
# Example: {{DOMAIN}} (for better performance and consistent domain)
# You MUST add this domain to Firebase Console > Authentication > Settings > Authorized domains
VITE_FIREBASE_AUTH_DOMAIN={{DOMAIN}}

VITE_FIREBASE_PROJECT_ID={{PROJECT_ID}}
VITE_FIREBASE_STORAGE_BUCKET={{PROJECT_ID}}.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
VITE_FIREBASE_APP_ID=your_app_id
VITE_FIREBASE_MEASUREMENT_ID=G-XXXXXXXXXX

# Firebase Realtime Database URL (required for real-time features)
# Format: https://[PROJECT_ID]-default-rtdb.[REGION].firebasedatabase.app
VITE_FIREBASE_DATABASE_URL=https://{{PROJECT_ID}}-default-rtdb.europe-west1.firebasedatabase.app

# App Configuration
# Cookie prefix for tracking (e.g., free trial tracking)
VITE_COOKIE_PREFIX=app

# Optional: Set to 'true' to use mock data instead of real API
# VITE_USE_MOCK=false

# Firebase App Check (reCAPTCHA Enterprise) - RECOMMENDED for security
# Protects your app from abuse and bot attacks
# Get this from: Firebase Console > App Check > Apps > Web app
# Or: Google Cloud Console > Security > reCAPTCHA Enterprise
# See docs/setup/firebase-app-check-setup.md for detailed setup instructions
VITE_RECAPTCHA_ENTERPRISE_SITE_KEY=your_recaptcha_enterprise_site_key_here
