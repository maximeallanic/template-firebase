/**
 * Application configuration
 *
 * Toggle USE_MOCK_API to switch between mock and real Firebase backend
 */

// Set to true to use mock data (no Firebase/Anthropic API needed)
// Set to false to use real Firebase Functions
export const USE_MOCK_API = false;

// You can also use environment variables
// export const USE_MOCK_API = import.meta.env.VITE_USE_MOCK === 'true';
